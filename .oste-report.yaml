task_id: "fix-click-to-focus"
status: "success"
summary: "Add focusGhosttyBundleAndTmuxWindow — two-step focus via open -a + tmux select-window, eliminating System Events permission dialog and restoring correct tmux window targeting"
files_changed:
  - "src/ghostty/window-focus.ts"
  - "src/extension.ts"
  - "test/ghostty/window-focus.test.ts"
tests_passing: true
next_steps: "Pre-existing tree provider test failure (AgentStatusTreeProvider > inline diff summary > task item description excludes diff when getDiffSummary returns null) from commit 6e4e18c needs investigation by the review-tracker task owner"
