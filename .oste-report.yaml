task_id: "prompt-summary-persist"
status: "success"
summary: "Persist prompt_summary in tasks.json at registration and use as fallback in tree provider"
files_changed:
  - "src/providers/agent-status-tree-provider.ts"
tests_passing: true
next_steps: "Launcher change already committed in ghostty-launcher (f8a5ef0). Both repos in sync."
