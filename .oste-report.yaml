task_id: "fix-agent-diff-view-temp-files"
status: "success"
summary: "Fixed temp file leak in agent diff view: stable per-task dirs, startup cleanup, deactivate cleanup"
files_changed:
  - "src/extension.ts"
  - "test/commands/file-diff-temp-dir.test.ts"
tests_passing: true
next_steps: "No further action needed. The git URI scheme approach can be revisited in the future for even cleaner tab headers."
