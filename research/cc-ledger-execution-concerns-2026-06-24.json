{
  "concerns": [
    "[PAR-226] src/utils/agent-counts.ts::countAgentStatuses (OUTSIDE my file-set) is the pure metadata-only counter that drives the legacy status-bar item in src/services/agent-status-bar.ts. It still counts completed + review_status=pending as 'attention' with no filesystem/liveness context (it cannot probe). So the legacy status-bar badge can still over-count a stale projection, even though the Agent Status tree and the V2 unified counts (computeUnifiedSectionCounts) are now correct. Fully reconciling that surface needs a probe-aware path or a precomputed signal passed into agent-counts.ts \u2014 both outside this ticket's allowed file-set.",
    "[PAR-226] The ledger pointed at src/providers/agent-task-normalize.ts::laneRefUpdateToTaskRecord to 'handle review.state=pending lanes from lanes.json'. I verified ghostty-launcher/scripts/laneref-update-schema.json: the lane_ref envelope has NO review/review_state/pending_review_path fields. A lanes.json projection row therefore can never carry review-pending metadata or a receipt path, so it can never become a stale review projection by this classifier and there is nothing to project. I left agent-task-normalize.ts unchanged (no field exists to map); changing it would be speculative and out of contract.",
    "[PAR-226] The inline 'review receipt missing' chip (getTreeItem, ~line 9897) suppresses itself when review_status==='pending' but NOT when only review_state==='pending'. A review_state=pending stale projection now sits in limbo but may still render the 'review receipt missing' chip text \u2014 cosmetic only, not part of the badge/grouping acceptance criteria, and not asserted by any test.",
    "[PAR-152] test/integration/suite/infrastructure-health.test.ts:45 asserts the live-rendered state is one of exactly five [OK,WARN,DEGRADED,STALE,DOWN]; it is OUT of my allowed file-set so I did not touch it. AUTH only renders on a real 401/403 from the live hub (which answers OK in a healthy host), so this assertion will not fire on AUTH in normal CI and is not broken by this change. A future live-auth smoke would need that list extended to include AUTH.",
    "[PAR-152] src/services/integration-test-api.ts exposes infrastructureHealthStatusText generically via getStatusText(), so it needs no change for the new state \u2014 confirmed it does not enumerate states.",
    "[PAR-152] The 403 regression test passes a non-JSON body ('forbidden'); production short-circuits 401/403 before calling response.json(), so this is safe, but it relies on that ordering staying intact.",
    "[PAR-149] PAR-149 is still marked Backlog in Linear; flipping it to Done is a tracker mutation outside this task's allowed file-set (research/ only) and outside the no-mutation constraints, so it is recorded as a follow-up rather than performed.",
    "[PAR-149] Several children remain open under their own scope: CC-002/PAR-152 (partial - auth-failed state + documented health-truth contract still missing), CC-004/PAR-156 (partial - dry-run/apply receipt files + mirror-path design spec missing), CC-005/PAR-157 (todo - conductor daemon spec), CC-006/PAR-158 (partial - no consolidated dogfood report). None block CC-000's structural deliverable but they are why the umbrella is not fully wrapped.",
    "[PAR-149] Naming collision risk: research/RESULT-cc-004-preview-proof-cut-20260611.md shares the 'cc-004' string but is an unrelated launcher-quarantine VSIX/rc.56 task, not the CC-004/PAR-156 closeout - noted so future readers do not mistake it for the orchestration receipt.",
    "[PAR-149] The orchestration plan's source paths reference /Users/ostehost/... (a different host) while this repo lives under /Users/ostemini/...; cosmetic provenance drift only, the artifact content is intact.",
    "[PAR-237] Test root is locked to ./test by bunfig.toml; the new pure helpers (evaluateDaemonSmoke, evaluateRepoParity, receiptFileName, collectGateEvidence) are exported but currently have no in-tree test, so Knip may flag them as unused exports until the scaffold is applied. Per the CCREL closeout docs, Knip warnings are informational (not gate failures), and the export pattern mirrors the already-tested runNodeReadinessCheck/collectSinceSection.",
    "[PAR-237] evaluateDaemonSmoke uses a tolerant JSON shape (running/alive/ok/state plus socket/socketPath/pid) because the exact `openclaw daemon status --json` schema could not be verified against a live daemon in this environment; the doc flags this and the schema should be confirmed on the hub before the first integrated gate run.",
    "[PAR-237] runRepoParityCheck compares against origin/main without fetching first \u2014 it trusts the local origin/main ref to be current. If the operator has not fetched, a stale origin/main could mask real drift. The cut-preview preflight already requires clean trees, which mitigates but does not fully cover the fetch-staleness case.",
    "[PAR-237] Many sibling-agent edits are present in the working tree concurrently (package.json, src/**, scripts/svg-to-png.ts); the orchestrator's post-batch gate run is the authoritative typecheck/test, since I was instructed not to run tsc/bun/just.",
    "[PAR-230] Activation wiring (extension.ts) is out of my file-set, so the command is contributed and fully implemented/tested but not yet invoked at runtime until that one call site is added.",
    "[PAR-230] Runbook URL is a placeholder under the existing dashboard.partnerai.dev host (mirrors openInfrastructureDashboard's hardcoded URL); confirm the real session-history recovery runbook/receipt URL before release.",
    "[PAR-230] 'Orphan session file' is conservatively defined as a `.trajectory.jsonl` with no surviving base `<id>.jsonl`. The live agents tree currently shows zero such orphans, so this branch is covered only by synthetic tests; broaden the definition if OpenClaw introduces other orphan shapes.",
    "[PAR-230] The real archive dir `~/.openclaw/archive/sessions-main-20260330-145824` holds ~970 deleted transcripts and each agent sessions dir holds hundreds-to-1550 files; the scan is synchronous readdir/stat. For the largest trees this is a few thousand stat calls per open. It runs only on explicit command open (not on a watcher/timer), so it is acceptable, but consider a cap or async scan if it is ever wired to auto-refresh.",
    "[PAR-230] The working tree has many concurrent sibling edits from other agents; I touched only my 5 files and my package.json diff is a single clean command insertion.",
    "[PAR-79] Display name is now derived from a user-controlled string but is only used as a tree-slot label and for debug logging, so there is no injection/escaping risk in this path.",
    "[PAR-79] package.json contributes commandCentral.project.name with no default; an empty string is treated identically to unset via the trim()+|| fallback, which matches expected UX but was not previously specified anywhere.",
    "[PAR-72] The DST regression test relies on Bun honoring a runtime process.env.TZ change (a documented Bun capability). If executed in a runtime/environment that ignores runtime TZ changes and defaults to UTC, the constructed dates are interpreted in UTC where there is no DST, so the test becomes always-green (still passes on the fixed code, just stops catching the bug). It never produces a false failure. No existing test in the repo sets TZ, so there is no established precedent to copy.",
    "[PAR-72] I could not run bun test/build/biome per the batch hard rules, so the fix was verified by reasoning about the arithmetic rather than execution. Line 40 was hand-wrapped to multi-line to match the file's existing Date(...).getTime() formatting and avoid a Biome reflow; the orchestrator's post-batch 'just fix' will confirm.",
    "[PAR-64] The existing integration suite's getDeletedFileTimestamp tests already pass under bun:test even with the old Bun.spawn code (Bun is defined there), so they never caught this Node-host-only bug \u2014 exactly the 'why tests miss this' gap noted in the ledger. My added tests now cover the Node path, but the suite as a whole still runs under Bun, not a true Node extension host.",
    "[PAR-64] getDeletedFileTimestamp is currently only referenced from its own definition in src/ (no production caller found via grep); it is invoked dynamically/by tests. The fix is correct, but if it is genuinely dead in production the underlying deleted-file timestamp feature may not be wired up \u2014 worth confirming separately (and Knip already flags it as unused).",
    "[PAR-64] The new default runner uses execFile's timeout to kill hung git; on timeout/kill execFile's callback receives an error and the function returns undefined (same observable contract as before). This is intentional but slightly different from the old manual Promise.race which rejected with 'Timeout' \u2014 behavior collapses to undefined either way via the catch.",
    "[PAR-63] The compile-time regression is the part that truly fails on the buggy code (a pure runtime test cannot, since the old optional type still allowed the builder to set `type`). If the project's `bun test` runs WITHOUT a separate tsc/typecheck step in the same gate, the type-level guard would not be exercised at test runtime; however the project's `just check`/`bun test` typechecks (tsc --noEmit) before tests per CLAUDE.md, so it is enforced.",
    "[PAR-63] src/utils/extension-discovery.ts defines an unrelated second `GitChangeItem` interface \u2014 a naming collision that could confuse future readers/greps, but it is out of scope and out of my file-set.",
    "[PAR-57] The vscode mock's Uri.file stores fsPath verbatim (no real win32 normalization), so the Windows scenario is simulated by passing backslash strings directly. This faithfully exercises the buggy code path (string containment on raw fsPath) but is not a true cross-platform integration test on a real Windows host.",
    "[PAR-57] Workspace keys derive from config.gitPath which originates from vscode.Uri.fsPath upstream; if any caller ever passes mixed-separator paths the normalization handles it, but no separate test covers a workspace key that is itself backslash-formed AND a forward-slash file (both-normalized covers it logically).",
    "[PAR-50] lsof -Fn can in principle emit multiple field lines; parseCwdFromLsof returns the first n-line, which is correct for the single-fd `-d cwd` query used here but would need revisiting if the args ever broaden.",
    "[PAR-50] isWithinProject does not resolve symlinks (uses path.resolve, not fs.realpath); a symlinked project dir vs symlinked cwd could mismatch. Out of scope for this ticket and matches the prior non-symlink-aware behavior, but noting it.",
    "[PAR-49] TEST DISCOVERY: my allowed file-set fixed the regression test path at src/ghostty/BinaryManager.test.ts, but bunfig.toml sets [test] root = './test' and package.json's `test` script enumerates ONLY test/ subdirectories. A co-located *.test.ts under src/ is therefore NOT picked up by `just test` / `bun run test` as currently configured, so this regression test will not run in CI/local gates unless either (a) it is moved/duplicated into test/ghostty/binary-manager.test.ts, or (b) src/**/*.test.ts is added to the test script globs. The natural home is the existing test/ghostty/binary-manager.test.ts which already has all the builders/mocks, but that file is outside my allowed file-set so I could not add the case there. Recommend the orchestrator either relocate this test into test/ghostty/ or extend the test globs.",
    "[PAR-49] scripts-v2/test-validate.ts (partition validator) scans test/ for orphaned tests; a src-co-located test may be flagged as un-partitioned or simply ignored \u2014 verify during the batch gate.",
    "[PAR-49] The wrapped error message changed from `Ghostty app validation failed after extraction \u2014 backup restored` to `Ghostty install failed (Ghostty app validation failed after extraction) \u2014 backup restored`. Existing assertions match on the substring `Ghostty app validation failed`, which is preserved, but any consumer/test matching the exact old full string would break (none found in repo grep).",
    "[PAR-75] isConfinedRelativePath uses POSIX path.isAbsolute on the test platform (darwin), which does not treat Windows drive-letter paths (e.g. C:\\\\...) as absolute. The '..'/empty-segment split on both / and \\\\ still catches traversal, and the URI path is forward-slash based after decode, so the realistic attack vectors (leading-/ absolute and ../ traversal) are covered. If Windows support ever matters, consider also rejecting drive-letter prefixes.",
    "[PAR-75] readGitRef() passes the decoded relativePath into 'git -C projectDir show ref:relativePath' without the same confinement; git resolves ref:path against the repo object DB rather than the host filesystem, so it is not a filesystem-escape vector and was out of scope for CP-39, but it remains an untrusted value flowing into a git invocation.",
    "[PAR-74] VS Code disposes context.subscriptions in reverse registration order; provider is pushed before treeView, so on shutdown the treeView is disposed before the provider. The provider's getTreeItem/getChildren are not called during disposal, so order is harmless here, but worth noting if future code makes provider.dispose() depend on a live TreeView.",
    "[PAR-74] GroupingViewManager.dispose() is still public and now overlaps with context-driven disposal; both paths are made idempotent, but any future caller relying on dispose() side effects firing more than once would be surprised (intentional per the ticket's 'run exactly once' guidance).",
    "[PAR-74] The regression test asserts the observable dispose() of the state subscription rather than asserting a tree refresh did/didn't fire, because the manager does not expose the provider (its design philosophy avoids test-only getters). This is the strongest assertion available without breaking encapsulation.",
    "[PAR-60] The regex character class [\\d.] permits malformed multi-dot tokens like '1.2.3' (parseFloat would yield 1.2). This matches the ledger's suggested pattern and is acceptable for trusted in-repo marketing SVGs, but it is not a strict number grammar.",
    "[PAR-60] The unit test exercises the pure parser only, not the live Chromium render path (parseViewBoxDimensions -> Playwright). The screenshot/2x output is not asserted end-to-end because that requires launching Playwright, which the orchestrator's gate environment may not support; the ledger's '2x scaled dimensions' AC is covered via the width*SCALE_FACTOR assertion on the parsed result.",
    "[PAR-60] I added a Math.ceil rounding on the Playwright viewport (slightly beyond the literal ledger pointers) because fractional viewports throw at runtime; without it, the decimal viewBox the ticket is about would still break during actual conversion.",
    "[PAR-56] Multi-segment dotfiles like `.env.local` are handled by standard path.extname (key becomes `.local`), which is unchanged prior behavior and not in scope for this ticket. If grouping `.env.local` under `.env` is desired, that would be a separate enhancement.",
    "[PAR-56] The known-dotfile behavior is driven entirely by membership in EXTENSION_DISPLAY_NAMES. Currently `.env` is the only single-segment dotfile in that table that benefits; adding future bare-dotfile names there will automatically opt them into this grouping, which is the intended design but worth noting.",
    "[PAR-52] TEST DISCOVERY GAP: My allowed file-set and the ledger both specify src/test/session-resolver.test.ts, so I created it there. But bunfig.toml sets [test] root = './test' and package.json's 'test' script enumerates only test/<dir>/ paths (test/discovery/ exists). A test under src/test/ will NOT be executed by 'just test'/'bun test' as configured. The pre-existing src/ghostty/BinaryManager.test.ts confirms this gap is not new. To actually run in the suite, this file should live at test/discovery/session-resolver.test.ts (alongside the other discovery tests) \u2014 but that path is outside my allowed file-set, so I did not create it there.",
    "[PAR-52] byNewestCreatedAt is still used as the pre-sort in listClaudeSessionCandidates and as the started_at===null fallback; that is intentional and preserves prior behavior, but it means the only correctness guarantee for the heuristic lives in selectClaudeSessionCandidateForTask's re-sort.",
    "[PAR-46] The allowed file-set listed test/helpers/vscode-mock.js but the actual source file is test/helpers/vscode-mock.ts (the .js is the ESM import specifier). No change to that file was needed \u2014 it already exposes ViewColumn (One:1/Two:2/Three:3) and the executeCommand mock \u2014 so this naming mismatch did not block the work.",
    "[PAR-46] URI assertions rely on object identity in practice (the command passes through the same MockUri instances the test stored on stagedChange), but I used .toString() comparison to stay robust even if the command were to reconstruct URIs, matching the sibling test's convention.",
    "[PAR-229] Querying a REMOTE node's repo for true cross-machine parity needs live cross-machine access this hub-side provider cannot perform safely; per the ticket KIND note this is implemented as explicit not-yet-queried/node-unavailable cards rather than fabricated facts. The remote-query transport remains future work.",
    "[PAR-229] No tree view/command was registered into the render/section dispatch in agent-status-tree-provider.ts: that file is being edited concurrently by sibling agents and full UI render wiring exceeds the smallest-correct-change bar. The receipt is exposed via the provider's public getSyncReadiness() API for a follow-up render step.",
    "[PAR-229] Did not run just/bun/tsc/biome per the edit-only batch constraint; orchestrator runs the gate. The os.hostname() || 'unknown' pattern mirrors the existing agent-task-classification.ts style to avoid biome nullish-on-non-null flags.",
    "[PAR-83] Allowed file-set lists scripts/build.ts, but that file does not exist in this repo; the real build is scripts-v2/dist-simple.ts (outside the set). I read it for the audit but did not edit it and did not fabricate a scripts/build.ts.",
    "[PAR-83] bunfig.toml sets frozen-lockfile=false; CI could silently mutate bun.lock. Hardening would require editing bunfig.toml, which is outside PAR-83's allowed file-set \u2014 not changed.",
    "[PAR-83] package.json has no packageManager field; pinning would harden the toolchain but requires an agreed exact Bun version, so deferred rather than guessed.",
    "[PAR-68] External caller src/providers/cron-tree-provider.ts:38 (out of my file-set) calls this.service.reload() and ignores the result. I kept reload()'s public signature as `void` specifically so this caller continues to compile and behave the same; no edit needed there.",
    "[PAR-68] Initial-render latency: with the old synchronous reload, the first data set was populated before start() returned; now it lands a tick later via the async CLI. Consumers (agent-status-tree-provider, cron-tree-provider) read getTasks/getFlows/getJobs on their own refresh cadence, so the UI updates shortly after activation rather than synchronously. I deliberately did NOT fire onChange after the initial reload because that would change long-standing semantics and break the existing watcher-callback-count tests (they assert exactly one onChange from a watcher event). Flagged as a future item instead of expanding scope.",
    "[PAR-68] OpenClawTaskService and OpenClawConfigService use the same start()-then-watch pattern with a synchronous execFileSync reload() but are explicitly OUT of PAR-68's scope (and out of my file-set), so they were left untouched. They carry the same blocking risk.",
    "[PAR-61] Behavioral choice: a successfully-parsed config whose top-level `agents` key is absent now clears the map (ledger flagged only the parse-failure path as the bug). This preserves the prior semantics for an operator who intentionally empties the config, while only the malformed/unparseable path retains last-known-good. If product intent is to also retain models on a valid-but-agents-less config, the `if (!agents)` branch would need to keep the map instead of clearing.",
    "[PAR-61] Did not run the test suite per orchestration rules (no bun test/build). Correctness verified by reading the diff and reasoning through the old-vs-new map state; orchestrator's post-batch gate will execute it.",
    "[PAR-47] Could not run the gate (just/bun test/tsc/biome are prohibited in this batched edit phase per instructions); correctness verified by reading source, type defs, exports, and mock helpers rather than execution. Biome 2.3.1 recommended ruleset does not enable noNonNullAssertion, but I avoided non-null assertions anyway for safety.",
    "[PAR-47] spyOn/mockImplementationOnce/mockRestore are not used anywhere else in this test suite (it favors mock.module/mock()); they are valid bun:test APIs and present in bun-types/test.d.ts, but this is the first usage in-repo.",
    "[PAR-47] The pre-existing test file (created by a sibling agent after the ledger snapshot) covers PAR-57 path containment, not PAR-47; I appended a separate describe block and left those tests untouched.",
    "[PAR-156] No regression test added: this is a design/spec (markdown) deliverable with no code change, so the orchestrator's regression-test rule does not apply.",
    "[PAR-156] The committed dry-run receipt file the intake doctrine prescribes (~/.openclaw/linear/intake/receipts/...dry-run...) is not duplicated into research/. Regenerating it requires a LIVE Linear read against the Command Central project, which is outside this task's edit scope (research/ only) and must not be faked. The doc records this honestly; the plan hash + source hash (A.1) plus the clean apply read-back (A.2) are the committed evidence the gate was satisfied.",
    "[PAR-156] The plan JSON's source.path uses a different home path (/Users/ostehost/...) than this checkout; the doc notes the plan is portable by hash, not absolute path, so it remains valid evidence here.",
    "[PAR-65] src/git-sort/deleted-file-tracker.ts:158 has a stale misleading comment 'storage handles INSERT OR IGNORE for duplicates' that reflects the now-removed write-once design; it is outside the allowed file-set so I did not touch it.",
    "[PAR-65] Per HARD RULES I did not run bun test / tsc; the orchestrator must run the full gate after the batch to confirm green.",
    "[PAR-65] The mock's queryByTimeRange/queryRecent/getStats iterate this.deletedFiles directly and are unaffected by the save() change; verified no other test in the suite depended on append/write-once accumulation (Query/Stats beforeEach blocks save once per repo).",
    "[PAR-53] isMissingFileError includes a message-substring fallback ('ENOENT' in err.message) so it works with the existing test mock that throws new Error('ENOENT') without a .code; real Node fs sets .code so the primary path is exact. This is intentional but slightly broader than a pure code check.",
    "[PAR-53] Per orchestrator rules I did not run bun test / tsc / biome; correctness verified by reasoning and grep only. Full gate runs in the batch.",
    "[PAR-51] The ledger's implementation_pointers suggested using node:path with path.relative()/path.resolve()/path.sep. I deliberately did NOT follow that approach: node:path is platform-locked, so on the darwin test runner it uses POSIX semantics and treats backslashes as ordinary chars (verified: path.relative('C:\\\\repo','C:\\\\repo\\\\src') returns '../C:\\\\repo\\\\src'), which would make the Windows regression tests fail. A separator-agnostic string helper is correct on every OS and satisfies the actual acceptance criteria (Windows matching works regardless of test-runner platform).",
    "[PAR-51] The realpath symlink sub-branches still call fsSync.realpathSync, which on a real Windows machine would correctly resolve native backslash paths; the isPathInside results feeding them are now separator-agnostic, so behavior is consistent across platforms.",
    "[PAR-76] The timestamp guard relies on Date.now() returning distinct values for two marks; if two real marks land in the same millisecond the guard collapses to has()-like behavior, but in that case both marks share the same TTL deadline so an early clear is harmless. The regression test forces distinct timestamps to exercise the real defect.",
    "[PAR-76] Per repo rules I did not run just/bun test/tsc; the orchestrator runs the gate after the batch. The setTimeout-spy cast and Date.now override are standard and type-safe but were not compiled here.",
    "[PAR-80] tsconfig.json `include` is [\"src/**/*\",\"scripts/**/*\",\"test/**/*\"] and does NOT list scripts-v2/**, so the new script is type-checked only transitively via its test import \u2014 identical to how the existing scripts-v2/prerelease-gate.ts is covered, so this is consistent, not a regression.",
    "[PAR-80] Biome may reformat the new files (line wrapping in long comments / template strings); I matched tab indentation and the existing JSDoc block-comment style, but `just fix` in the orchestrator gate could adjust minor formatting.",
    "[PAR-80] git status --porcelain=v1 -z rename entries emit new\\0orig; the classifier advances past the orig field. This was reasoned-through, not exercised by a live rename in tests (the unit tests drive synthetic -z strings) \u2014 a real-repo rename smoke via `just preserve-audit` would add belt-and-suspenders confidence but is out of the edit-only scope.",
    "[PAR-78] Per instructions I did not run bun test/tsc/biome, so the new test and edits were not executed locally; the orchestrator's gate must confirm green. I verified the assertion regexes against the exact post-fix source strings by hand.",
    "[PAR-78] Build-only behavior with an ALREADY-existing version: dist-simple only builds the production VSIX inside `if (!versionExists)`, so `bun run build` on a version that already has a releases/ VSIX will only produce the dev VSIX and skip the production candidate (it never reaches the build-only branch). That mirrors pre-existing dist behavior and was out of scope for the side-effect fix, but means `build` is not guaranteed to emit a fresh prod VSIX for an already-released version.",
    "[PAR-78] The summary/dry-run console messaging was adjusted for accuracy but is cosmetic; the load-bearing change is the side-effect guards.",
    "[PAR-66] The new block depends on the existing harness in test/tree-view/_helpers/agent-status-tree-provider-test-base.ts, which has module-scope side effects (clobbers AgentStatusTreeProvider.prototype.readRegistry and registers node:fs/node:child_process/port-detector module mocks). This is the established, proven pattern (diff-notifications test relies on it identically), and the file-level mock.restore() + per-test createProviderHarness() re-establishment is what makes it safe. Importing this helper into test/events/ couples that directory to a tree-view helper, but it is import-only (allowed by the file-set) and avoids re-implementing fragile mock infra.",
    "[PAR-66] elapsed assertions ('5m'/'3m') are deterministic because formatElapsed truncates to whole minutes and the started_at offsets (5/3 minutes) are comfortably inside a minute boundary; no real timers are involved.",
    "[PAR-66] I could not run bun test / tsc per the task's hard rules, so type-check and runtime pass are gated by the orchestrator after the batch.",
    "[PAR-62] I could not run bun test/tsc/biome per the orchestration rules (edit-only), so the changes are validated by reading only; the orchestrator must gate the batch.",
    "[PAR-62] Sibling services AcpSessionService/CronService/TaskFlowService have the identical synchronous-reload bug (that is PAR-68 / [CP-29]), but they are outside this file-set so I left them untouched.",
    "[PAR-62] The async reload mildly changes timing semantics for any future caller that assumed reload() completes synchronously before reading getTasks(); current callers do not, so no breakage, but it is worth noting.",
    "[PAR-48] The working tree currently has ~60 files modified by concurrent sibling agents (provider-factory.ts shows ' M', test/factories/ is '??' untracked); the PAR-48 fix is uncommitted and intermingled with PAR-57/PAR-47 changes in the same file, so it has not yet passed the orchestrator gate (tsc/biome/bun test).",
    "[PAR-48] The test file's docblock attributes the getProviderForFile Windows tests to 'CP-16 / PAR-57' rather than PAR-48/CP-05; functionally identical, but a future reader auditing PAR-48 closeout may not find an explicit PAR-48 marker.",
    "[PAR-48] Normalization is separator-only; it does not case-fold Windows drive letters or paths. PAR-48's stated AC only requires separator-aware containment, so this is in-spec, but truly case-insensitive Windows path matching is not handled (consistent with the rest of the codebase).",
    "[PAR-69] The regression rides inside the live-VS-Code suite scenario tree-view-renders.test.ts run() (the allowed file), calling the pure getTreeSnapshotForProvider directly with a fake provider \u2014 it does not need the live host but executes within that scenario. A more isolated home would be a bun:test unit file like test/integration/lane-registry-projection.test.ts style, but that path is outside the allowed file-set.",
    "[PAR-69] The fake provider uses `fake as unknown as AgentStatusTreeProvider` (double cast). This is the standard test-double escape and is not `as any`, but it is a structural shortcut: if AgentStatusTreeProvider's getChildren/getTreeItem/getTasks signatures change, the fake will silently drift.",
    "[PAR-69] matchesRequiredLabel and serializeNode each call provider.getTreeItem on escaped nodes, so an escaped required-label node has getTreeItem evaluated twice (once in isRequiredLabelNode, once in serializeNode). This mirrors the pre-existing requiredTaskId double-evaluation pattern and is acceptable for a read-only test API, but is a minor redundancy.",
    "[PAR-77] The two provider methods (getCurrentChanges vs getCurrentChangesUnfiltered) currently return identical raw repo.state data, so this is a latent contract-alignment fix, not an actively-reproducing user-facing bug today. The regression value is guarding against a future change to getCurrentChanges that adds filtering.",
    "[PAR-77] Did not run just/bun test/tsc/biome per task rules (concurrent sibling agents); the orchestrator must gate. The test relies on a real 600ms setTimeout wait for the 500ms debounce, which adds ~0.6s to the suite but uses no fake timers to stay robust.",
    "[PAR-77] The typeof fallback branch (provider.getCurrentChanges()) is statically unreachable given SortedGitChangesProvider always declares getCurrentChangesUnfiltered; it is intentional defensive code per the ledger pointer and compiles cleanly.",
    "[PAR-73] Did not run bun test/typecheck per the no-build instruction; orchestrator must gate. Reasoned through both regression tests: they fail on pre-fix code and pass post-fix, and existing tests are unaffected because the mock returns default=true when showProjectIcon is absent.",
    "[PAR-73] The guard uses `=== false` (strict): if showInStatusBar were ever undefined it falls through to show, which matches the contribution default of true and is the intended behavior.",
    "[PAR-55] The regression test asserts the new session is targeted but does not assert the OLD session stops being polled (the stale timer is cleared via stopStreaming, but a negative assertion like 'valid-session-1 not captured after the swap' was intentionally omitted because both callbacks share the same capturedSessionIds array and ordering across fires would make a strict negative brittle). The positive assertion plus the stopStreaming clearInterval is sufficient to prove retargeting; a stricter test could spy on clearInterval.",
    "[PAR-55] On a session swap, lastLineCount for the task is reset (stopStreaming deletes it), so the first poll of the new session re-emits its visible buffer from line 0. This matches expected behavior for a brand-new session pane but is a minor behavior note rather than a bug.",
    "[PAR-55] mock.module('node:child_process') is registered at module scope and persists across tests in the file (Bun's mock.restore does not undo mock.module); existing tests stub startStreaming to no-op so they never hit execFileSync, so there is no cross-test interference \u2014 but this relies on the same Bun semantics the sibling test files already depend on.",
    "[PAR-81] Per task rules I did not run bun test / tsc / biome (concurrent sibling agents; orchestrator gates after the batch). The new test is written against proven existing bun:test helpers, but it has not been executed in this session.",
    "[PAR-81] The fix is intentionally minimal and scoped to the gate's stated '.vscodeignore mirror' contract. out/ and prompts/ are gitignored but absent from .vscodeignore, so I did not add them to the gate to avoid scope creep beyond the AC and the gate's documented semantics; they are recorded as future_items.",
    "[PAR-84] Marked partial only for the LIVE end-to-end refusal: an actual scripts/release.sh run that exit 1s against a deliberately-wrong origin, and a live `just prerelease-gate --require-push-target` run, both need a real/mutable remote and were out of scope for this edit-only lane. All pure logic + gate runner + bash assertion are fully implemented and tested with injected inputs / temp-git fixtures.",
    "[PAR-84] The guardrail is opt-in (--require-push-target) so it does not change existing CI / just cut-preview / just prerelease-gate behavior. It is NOT yet wired into a justfile recipe (justfile is outside the allowed file-set) \u2014 a follow-up should add it to the integrated gate recipe and/or the cut-preview flow for it to run automatically.",
    "[PAR-84] normalizeGitHubRemote handles github.com remotes only; non-github hosts return '' and are reported as 'could not parse' (treated as a refusal), which is the intended safe default for this repo but would need extension if a non-github remote is ever legitimately used.",
    "[PAR-84] The bash normalize_github_remote duplicates the TS normalization logic (two implementations of the same contract). They were verified to agree on the test matrix, but future url-shape changes must be kept in sync in both places.",
    "[PAR-67] Per the allowed file-set I could not extract a shared watch-with-retry helper outside the three services, so the resilient-watcher logic (tryInstallWatcher/scheduleWatchRetry/stopWatchRetry/handleWatcherError) is duplicated near-identically across cron-service.ts, acp-session-service.ts and taskflow-service.ts. The ledger itself suggested a shared helper as the natural fix.",
    "[PAR-67] Regression test coverage lives only in cron-service.test.ts (the only test file in my allowed set). acp-session-service and taskflow-service got the same code change but no dedicated watcher-retry test; the ledger noted those two have no existing dedicated watch tests.",
    "[PAR-67] Could not run bun test/tsc/biome per the no-build constraint; relied on matching existing patterns and noUncheckedIndexedAccess/noPropertyAccessFromIndexSignature rules manually. Orchestrator should gate.",
    "[PAR-44] Did not run bun test / tsc / biome per the hard rules (orchestrator gates after the batch). Static review only: helpers use typed optional chaining with no index-signature property access, so noPropertyAccessFromIndexSignature/noUncheckedIndexedAccess do not apply.",
    "[PAR-44] Treated the launcher path as the intended mitigation (matches CP-01 security intent: it drives process execution). The ledger pointer listed 'or remove if machine-scope is the intended mitigation' as an alternative; I chose restriction over removal because untrusted workspaces should not silently exec a workspace-overridable launcher path even though current scope is machine.",
    "[PAR-228] getNodeStatusGroup is cache-only, so on the very first render cycle (cold attention cache) a benign pane reads as unknown and is briefly grouped as attention; it converges to limbo on the next refresh once getTreeItem warms the cache. This is the same two-phase contract the existing getCachedTerminalTaskLivenessEvidence already relies on and is fail-safe (attention preserved when in doubt).",
    "[PAR-228] The render-path badge uses the live subprocess liveness probe while getNodeStatusGroup uses cache-only liveness; a one-cycle divergence is possible but converges, matching existing behavior.",
    "[PAR-228] I added behavior-neutral stubs (capturePaneSnippet->null, classifyPaneAttention->unknown, isBenignLivePane->false) to five sibling tree-view tests that mock tmux-pane-health.js, otherwise the provider's new imports would resolve to undefined and crash. These stubs preserve each test's prior grouping/badge assertions. If a sibling agent is concurrently editing those same test files there could be a merge conflict.",
    "[PAR-228] The completion/prompt/interactive regexes are heuristic; exotic prompt themes or non-English prompts may classify as unknown (-> attention preserved, never wrongly suppressed). Tuning can follow from real captures.",
    "[PAR-70] flushPersistence() is a new public method; knip (run by `just check`) might flag it as unused if it does not detect class-member usage from test files. It is genuinely consumed by the new regression tests and has a real operational purpose (awaiting durability before deactivate), so it should be retained, but the orchestrator's gate may surface a knip warning to triage.",
    "[PAR-70] The regression tests rely on deterministic setTimeout ordering for distinct positive delays (40/25/10ms). This is deterministic in practice in bun:test but is timing-based; if ever flaky under heavy CI load the delay spread could be widened.",
    "[PAR-70] validateAndCleanFilter and setExtensionEnabled remain synchronous (void schedulePersist) \u2014 callers that need durable persistence must now opt in via flushPersistence(); no call sites were changed outside the allowed file-set to wire that in.",
    "[PAR-227] package.json is in my allowed set but I did not need to touch it; the M package.json in git status is pre-existing sibling-ticket work, not mine.",
    "[PAR-227] The working tree was already dirty with many sibling-ticket changes when I started (initial 'clean' gitStatus snapshot was stale) \u2014 agent-status-tree-provider.ts shows 362 insertions, of which only ~48 are my GC lines layered on top of PAR-226/PAR-228 work already present.",
    "[PAR-227] The default projection ingest read is opt-in (CC_LANE_GC_RECEIPT) to avoid coupling existing projection tests (worksystem-lanes-projection / lane-registry-projection, which call the real readRegistry and do not mock node:fs) to an ambient receipt file. A real deployment must set the env var (or a future config setting) to activate live consumption.",
    "[PAR-227] I kept two reconcile implementations (the tested exported applyGcReceiptReconciliation in agent-task-normalize.ts and an inline reconcileLanesAgainstGcReceipt in the provider) because the provider deliberately maintains parallel inlined normalize logic and does not import agent-task-normalize at runtime; importing it would create a (type-only-erased, but still avoidable) provider<->normalize edge.",
    "[PAR-227] I could not run just/bun/tsc/biome per the rules; type-safety (noUncheckedIndexedAccess, noPropertyAccessFromIndexSignature, no as any, type-guard narrowing for ReconciledGcVerdict) and biome import-ordering were verified by manual review only. shellcheck was run on the new script (clean).",
    "[PAR-45] autoDismissSeconds was removed rather than wired because the VS Code API cannot programmatically auto-dismiss showInformationMessage notifications; this is a user-facing contribution removal (the setting never functioned, so no behavior is lost, but anyone who had it set in settings.json will see an 'unknown setting' lint until they remove it).",
    "[PAR-45] The runtime cron.enabled toggle updates the view's `when` context key (show/hide) but does NOT tear down or recreate the CronService/CronTreeProvider/commands mid-session \u2014 full enable->disable lifecycle disposal on the live instance is not implemented (the gitSort pattern it mirrors only enables/disables in place too). A window reload fully applies the change; the view correctly hides immediately via the context key.",
    "[PAR-45] The regression test only exercises the disabled path (hermetic, no subprocess). The enabled-default path still imports the real CronService whose start() calls execFileSync('openclaw'); I deliberately did not assert on that path to avoid spawning subprocesses in the unit suite, consistent with the existing provider tests using FakeCronService.",
    "[PAR-58] The fix lives in the working tree only \u2014 HEAD (c17ef05) still contains the stale buggy restrictedConfigurations and the test file at HEAD has zero untrustedWorkspaces assertions. git diff confirms package.json (+1/-2 keys) and the test (+50 lines) are uncommitted. Per git-safety rules I did not commit; the orchestrator must include both files when it gates/commits, or the fix will be lost.",
    "[PAR-58] The package.json working-tree diff also contains UNRELATED changes from sibling tickets (removal of commandCentral.notifications.autoDismissSeconds; addition of the commandCentral.openSessionHistoryRecoveryRisk command \u2014 PAR-230 prior art). These are outside PAR-58's scope but are in the same file; the orchestrator should be aware the package.json diff is not solely CP-17.",
    "[PAR-58] Ledger lists PAR-58 depends_on_active PAR-57 \u2014 PAR-57's content was not inspected here; if PAR-57 also touches restrictedConfigurations there could be a sequencing/merge interaction, though the current working-tree state already reflects the correct end-state for CP-17.",
    "[PAR-157] The OpenClaw *.workflow.md write-gate the design hinges on lives in ~/projects/config/openclaw/conductor/workflows/ (outside this repo and outside my edit scope). The doc names it as the precondition (dispatchEnabled:false / trackerWritePolicy:none / linearMutationAllowed:false) and requires an operator-approved flip before any live dispatch/mirror; it does not and cannot flip it.",
    "[PAR-157] PAR-156 / CC-004 (the mirror-contract dependency this spec builds against) is marked depends_on_active, not done. The CC-004 mirror design doc exists and is committed (research/DESIGN-cc-004-linear-intake-mirror-contract-2026-06-23.md), so the contract is referenceable, but if CC-004 changes its mirror receipt shape, \u00a75/\u00a76 references would need a sync.",
    "[PAR-157] This is a paper design: no daemon code, no live tracker poll, no live launcher run was exercised (out of edit scope and not faked, matching the CC-001/CC-002/CC-004 'needs live infra' convention).",
    "[PAR-82] The new manifest gate validates the repo package.json at CLI run time, not the package.json inside the built .vsix zip; they are identical at packaging time but full hermeticity would parse extension/package.json from the zip.",
    "[PAR-82] getTerminalInfo (read-only --parse-name/icon/session lookup) still spawns the launcher and is intentionally NOT trust-gated, to keep the Agent Status tree rendering in untrusted workspaces. If launcher --parse-* is ever shown to execute project shell it must also be gated.",
    "[PAR-82] WorkspaceTrustRequiredError propagates to callers in extension.ts (outside my file-set, untouched); VS Code's command infrastructure surfaces it as an error notification, consistent with how Launcher* errors already propagate. I could not add a friendlier caller-side toast without editing extension.ts.",
    "[PAR-82] The working tree contains many in-flight sibling-ticket changes; I edited only my allowed files. The existing test/package-json/manifest-contract.test.ts (out of my set) already asserts ghostty.launcherPath is restricted and the stale terminal.* keys are absent, and its type allows supported:'limited', so my changes do not break it.",
    "[PAR-59] The ledger's implementation_pointers suggested an alternative richer fix (retain aged agents and tag them with stuck/stale STATUS METADATA so the UI surfaces them as 'stuck'). I implemented the narrower, task-guidance-aligned fix (probe liveness; keep live PIDs visible, prune dead ones) because DiscoveredAgent has no status/stuck field today and adding one would require editing src/discovery/types.ts and downstream consumers (agent-registry, tree provider) which are OUTSIDE my allowed file-set. The current fix keeps a live external agent visible as a normal discovered agent rather than a distinctly-styled 'stuck' node.",
    "[PAR-59] The two reconstructed scanners in unrelated tests (worktree test ~line 823 + cwd-internal-tool tests) use the default real process.kill probe, but all of them use freshly-started (non-aged) PIDs, so isStaleProcess returns false before ever reaching the probe; behavior is unchanged. Verified no other ProcessScanner construction relies on age-based pruning.",
    "[PAR-158] PAR-158 is fundamentally a dogfooding/process meta-task (run a real Linear issue end-to-end, then write a consolidated CC-006 closeout report). I implemented the concrete code gap it pointed at \u2014 cross-project OpenClaw issue rendering \u2014 but I could NOT (and per file-set rules must not) close the issue lifecycle, run live OpenClaw issues, or author the closeout report under research/ (those files are outside my allowed set).",
    "[PAR-158] The new rows render whatever the OpenClawTask projection supplies; if upstream OpenClaw/launcher does not yet populate trackerKind/issueUrl/execNodeName for a given lane, the rows are simply omitted (by design). I did not verify the live upstream actually emits these fields for real PAR-* lanes \u2014 only that the projection renders them correctly when present (fixtures + tests).",
    "[PAR-158] I extended the shared createTask() override whitelist in the test file. It is additive (all new fields optional) so existing callers are unaffected, but it does widen the inferred ReturnType<typeof createTask> the orchestrator's typecheck will exercise across all tests in this file.",
    "[PAR-231] The manifest-contract.test.ts (outside my file-set) asserts every contributed command has a source implementation and every referenced command is contributed; my literal registerCommand string + contributes.commands entry satisfy both checks, but I could not run the gate to confirm.",
    "[PAR-231] The closeout uses the existing markTaskFailedInRegistryMap (status->failed) as the non-destructive reconcile primitive because agent-task-registry.ts is outside my file-set; this records a distinct PANE_CLOSEOUT_STATUS_DESCRIPTION reason but reuses the 'failed' status. If product wants a dedicated 'closed_out' status it would need a new helper in agent-task-registry.ts (not in my allowed set).",
    "[PAR-231] The context-menu entry is gated on agentTask.running rows; if the launcher records benign stale shells under a non-running contextValue, the affordance would need that status added to the when-clause.",
    "[PAR-54] Reused the test file's existing node:fs mock, whose readFileSync passes through to real fs for any non-session-file basename; the new test relies on that pass-through to read the real temp fixtures (tasks.json / extra-tasks.json), consistent with the existing legacy-enabled test.",
    "[PAR-54] readLauncherTasks merges via array concatenation with no cross-file task-id dedup. The richer provider path (readRegistry) does suffix-key dedup on id collisions; this provider only feeds suppression/stale-matching predicates (matchesLauncherTask / isSuppressedByLauncherTask), which iterate and short-circuit, so duplicate ids across registries are harmless there. Mentioning in case a future consumer expects unique ids from this provider."
  ],
  "future_items": [
    "[PAR-226] Pass a precomputed stale-projection / not-actionable flag (or reviewedTaskIds-style context) into src/utils/agent-counts.ts so the legacy status-bar badge agrees with the tree's getNodeStatusGroup for stale review projections \u2014 currently they can diverge.",
    "[PAR-226] Wire the M3 sectionFromSignals engine into the render/badge path and have its signal builder compute reviewPipelineBroken excluding stale projections (contract now documented on V2SectionSignals.reviewPipelineBroken).",
    "[PAR-226] If the Work System lanes projection (lanes.json / laneref-update-schema.json) ever starts carrying review lifecycle (review_state, pending_review_path), extend laneRefUpdateToTaskRecord to map those fields and re-validate the stale-projection classification for projection rows.",
    "[PAR-226] Consider unifying the 'review receipt missing' chip gate to also suppress on review_state==='pending' stale projections for visual consistency with the limbo routing.",
    "[PAR-152] Extend test/integration/suite/infrastructure-health.test.ts state allow-list to include AUTH so a future live auth-failure smoke can assert it (out of this ticket's file-set).",
    "[PAR-152] Model the deferred deep node\u2194hub link-liveness dimension (websocket session up/down independent of /readyz) once a two-machine topology can be staged; currently only /readyz reachability + scope labeling cover node connectivity.",
    "[PAR-152] Model launcher-execution health on its owning surface (Agent Status tree / Ghostty Launcher), per Decision 3 of the contract.",
    "[PAR-152] Consider exposing summaryFreshnessMs / auth behavior as user settings if operators need to tune the 10m freshness window or auth-failure rendering.",
    "[PAR-149] Flip PAR-149 Backlog -> Done in Linear now that the structural deliverable is met and receipted.",
    "[PAR-149] Write per-child closeouts for CC-002/PAR-152, CC-004/PAR-156, CC-005/PAR-157, or formally mark CC-006/PAR-158 superseded by the PAR-189..195 dogfood runs.",
    "[PAR-149] Update WORK-LEDGER.md line ~34 to check off the PAR-149 worklist entry and cite this closeout receipt (left untouched here since WORK-LEDGER.md is outside the allowed research/ file-set).",
    "[PAR-237] Apply research/PAR-237-CCREL-05-regression-tests.ts.txt into test/scripts-v2/{prerelease-gate,release-digest,verify-vscode-extension-consumption}.test.ts (single lane with test/ access).",
    "[PAR-237] On the hub: run `just prerelease-gate-integrated` at the next RC HEAD, confirm daemon smoke + node readiness + repo parity are green, then cut rc.71.",
    "[PAR-237] Generate and commit dual-host consumption receipts vscode-consumption-0.6.0-rc.<N>-hub.json and -node.json via the new --node-label/--receipt-dir flags.",
    "[PAR-237] Confirm the real `openclaw daemon status --json` field names against evaluateDaemonSmoke's tolerant set and tighten if the live schema is stable.",
    "[PAR-237] Consider having `just cut-preview` invoke prerelease-gate-integrated on the hub (behind a host guard) so integrated parity is enforced by default rather than opt-in.",
    "[PAR-237] Optionally add a `git fetch origin main` (or a freshness assertion) before runRepoParityCheck to avoid stale-origin false-greens.",
    "[PAR-230] Wire the panel into activation: in src/extension.ts construct the service+panel and call registerSessionHistoryRecoveryCommands, pushing disposables + panel onto context.subscriptions (the out-of-set follow-up).",
    "[PAR-230] Add an Agent Status / Command Central view title or context-menu entry so the recovery-risk panel is discoverable beyond the command palette.",
    "[PAR-230] Optionally add a status-bar or tree badge when atRisk is true (count of transcripts past retention) to passively warn operators.",
    "[PAR-230] Confirm and replace the placeholder runbook URL with the canonical session-history recovery runbook/receipt link.",
    "[PAR-230] Consider parsing the embedded deletion timestamp label as an alternate age source when file mtime has been normalized by a backup/restore tool.",
    "[PAR-230] Write the CCSYNC-05 research closeout doc under research/ once the feature is wired and verified end-to-end.",
    "[PAR-79] WorkspaceProjectSource does not subscribe to onDidChangeConfiguration, so changing project.name (or project.icon) likely requires a reload/refresh to update the slot label; consider wiring a config-change listener to trigger loadProjects (out of scope for this ticket).",
    "[PAR-79] Consider documenting the commandCentral.project.name behavior (used as Agent Status slot display-name base, falls back to folder basename) in user-facing docs/README alongside project.icon.",
    "[PAR-72] Consider a shared TZ-fixing test harness (e.g. a helper or a TZ-pinned test invocation) so future date/timezone-sensitive tests are deterministic across local macOS and CI (likely UTC) runners.",
    "[PAR-72] Audit other timestamp-bucketing/age calculations (e.g. the separate DAY_MS-based staleness math in src/providers/agent-status-tree-provider.ts around lines 5188-5193) for similar DST/fixed-24h-multiple assumptions if calendar-accurate boundaries matter there.",
    "[PAR-64] PAR-65 is blocked by PAR-64 per the ledger blocks_active; this fix should unblock it \u2014 re-verify PAR-65 next.",
    "[PAR-64] Consider adding a CI test lane that runs the git-timestamps tests under real Node (not just Bun) to genuinely guard against Bun-only globals leaking into extension-host code paths.",
    "[PAR-64] Audit for other Bun-only globals (Bun.*, Bun.spawn, Bun.file, new Response on process streams) across src/ that could similarly fail in the Node extension host; git-timestamps.ts was the only remaining Bun.spawn, but a broader sweep would prevent recurrence.",
    "[PAR-63] Consider renaming or namespacing the unrelated GitChangeItem interface in src/utils/extension-discovery.ts to avoid confusion with the tree-element GitChangeItem.",
    "[PAR-63] Optionally add a lightweight helper-test in test/types/tree-element-helpers.test.ts that round-trips a no-discriminant object through getChildrenForElement to document defensive behavior (currently covered indirectly).",
    "[PAR-63] WORK-LEDGER.md entry '#14 \u00b7 PAR-63' is still unchecked; mark it done after the orchestrator's gate passes (out of my file-set).",
    "[PAR-57] Write the research closeout doc for CP-16/PAR-57 (the ledger evidence noted rg 'CP-16' research/ only hits cc-work-ledger.json and WORK-LEDGER.md:62 still lists it unchecked) \u2014 out of my allowed file-set.",
    "[PAR-57] PAR-57 blocks PAR-58 and PAR-59 per the ledger; those can proceed once this lands.",
    "[PAR-57] Consider a shared path-normalization utility (toPosixPath) since the `.replace(/\\\\/g, \"/\")` idiom now appears in at least three locations (diff-format.ts, agent-status-tree-provider.ts, provider-factory.ts).",
    "[PAR-50] PAR-51 (blocked by PAR-50) can now proceed since the boundary-aware containment is in place.",
    "[PAR-50] Consider a shared path-containment utility if other detectors (e.g. other lsof/process matchers) replicate the substring-vs-boundary pattern.",
    "[PAR-49] Consolidate the new regression test into test/ghostty/binary-manager.test.ts (or add src/**/*.test.ts to the bunfig/package.json test globs) so the PAR-49 coverage actually runs in the standard gate.",
    "[PAR-49] Consider also cleaning up the downloaded temp zip (zipPath) inside the failure path \u2014 currently on extraction failure the temp zip in os.tmpdir() is left behind (only removed on the success path). Minor disk-leak, not data-loss; out of scope for this ticket.",
    "[PAR-49] Consider an integration/e2e test exercising real `unzip` against a deliberately corrupt zip to validate the recovery path beyond mocks.",
    "[PAR-75] Consider centralizing the path-confinement check (decode + validate) in decodeRelativePath()/provideTextDocumentContent() so all refs share one validated path, per the secondary implementation pointer at provideTextDocumentContent():71 and decodeRelativePath():28.",
    "[PAR-75] Audit other providers/services that build filesystem paths from URI query/path params for the same traversal pattern (PAR-76 and PAR-77 are listed as blocked-by this ticket and may cover related surface).",
    "[PAR-74] Audit other view managers/providers in src/ui and src/providers for the same pattern (registering only the TreeView in context.subscriptions while a child provider holds event subscriptions) \u2014 this leak class may recur.",
    "[PAR-74] Consider a shared lint/convention or a small helper that registers a provider+treeView pair together to prevent future omissions.",
    "[PAR-74] No CLOSEOUT/RESULT doc exists in research/ for CP-36; once gates pass, record a closeout marker per the repo's research-ledger convention.",
    "[PAR-60] Consider tightening parseViewBoxDimensions to a strict SVG number grammar (e.g. -?\\d*\\.?\\d+(?:[eE][+-]?\\d+)?) to reject malformed tokens and support scientific notation.",
    "[PAR-60] Optional: add an integration test that actually renders a decimal-viewBox fixture through Playwright to assert PNG pixel dimensions, gated behind a Playwright-availability check.",
    "[PAR-60] No SVG fixtures were added to test/fixtures since the unit test inlines minimal SVG strings; a shared fixture set could be introduced if more svg-to-png coverage is added later.",
    "[PAR-56] Consider whether other common registered single-segment dotfiles (none currently in the table besides .env) or compound dotfiles (.env.local, .env.production) should also group under Environment.",
    "[PAR-56] The test file's filter-by-extension-command.test.ts mock at line 170 uses key 'ts' (no dot) for countExtensionsByWorkspace's mocked return; harmless since it's a mock, but a minor inconsistency with real output keys (always dot-prefixed).",
    "[PAR-52] Move src/test/session-resolver.test.ts to test/discovery/session-resolver.test.ts (or add src/test/ to the bunfig/package.json test globs) so the regression test is actually executed by the standard suite \u2014 currently the configured root='./test' excludes it.",
    "[PAR-52] Same coverage-execution gap likely affects the existing src/ghostty/BinaryManager.test.ts; audit for other colocated src/**/*.test.ts that never run.",
    "[PAR-52] Consider exporting selectClaudeSessionCandidateForTask (or a thin testable wrapper) so future unit tests can target the heuristic directly without fs fixtures.",
    "[PAR-52] PAR-52 blocks PAR-53 and PAR-54 per the ledger; those may now be unblocked.",
    "[PAR-46] WORK-LEDGER.md:94 still lists PAR-46/#29 as unchecked '[ ]' and research/cc-work-ledger.json marks it state=Backlog/verdict=todo; update the ledger checkbox/verdict and add a CP-03 closeout note now that the success-path assertions exist (out of this ticket's allowed file-set).",
    "[PAR-229] Render the SyncReadinessReceipt as an actual tree-row/section in the Agent Status tree (consume getSyncReadiness via the V2 section model) once concurrent edits to agent-status-tree-provider.ts settle.",
    "[PAR-229] Add a live remote-node query transport (e.g. via openclaw nodes / ssh) so a node's branch/upstream/ahead-behind/tree/dirty-count can move from not-yet-queried to queried with real facts.",
    "[PAR-229] Optionally fold the per-project receipt into the prerelease-gate hub-node repo-parity check so the gate and the UI card share one evaluator.",
    "[PAR-229] Register a read-only command (e.g. commandCentral.copySyncReadinessReceipt) to surface the machine-readable receipt JSON for operators.",
    "[PAR-83] Set frozen-lockfile=true for CI (or pass `bun install --frozen-lockfile` in CI) to guarantee the lockfile is never silently mutated \u2014 requires editing bunfig.toml (out of this ticket's file-set).",
    "[PAR-83] Add `packageManager: \"bun@<exact-version>\"` to package.json once a canonical Bun version is agreed, to machine-pin the toolchain.",
    "[PAR-83] Correct the CCSTD-04 standards kit's stale `scripts/build.ts` path reference to `scripts-v2/dist-simple.ts` (fix belongs in the kit, not this repo).",
    "[PAR-68] Apply the identical async-reload + coalescing + stale-result refactor to OpenClawTaskService (src/services/openclaw-task-service.ts) and OpenClawConfigService (src/services/openclaw-config-service.ts), which still use blocking execFileSync in start()->reload() and were out of PAR-68's scope.",
    "[PAR-68] Consider firing onChange once after the initial async reload completes so consuming views re-render exactly when first data lands (would require updating the watcher-callback-count tests to account for the extra initial notification).",
    "[PAR-68] Consider AbortController/timeout-cancellation for in-flight reloads on dispose() so a pending CLI child process is reaped when the service is torn down mid-reload.",
    "[PAR-61] PAR-62 is blocked-by PAR-61 per the ledger; it may build on the now-resilient reload semantics.",
    "[PAR-61] PAR-68 (separate ticket) covers converting reload() to async execFile with in-flight coalescing + stale-result protection across the three config services \u2014 out of scope here and out of this file-set.",
    "[PAR-61] Consider surfacing a one-shot warning (Logger/telemetry) when a parse error is swallowed so operators learn their config is malformed instead of it failing silently.",
    "[PAR-47] PAR-48 is blocked by PAR-47 (per ledger blocks_active) and may build on this in-flight map; ensure its work accounts for the new inFlight bookkeeping.",
    "[PAR-47] Consider applying the same in-flight-coalescing guard to any other factory/creation path that reads-then-awaits-then-writes a cache (audit for sibling TOCTOU patterns).",
    "[PAR-47] dispose() now awaits in-flight creations; if a creation can hang indefinitely, a future hardening could add a timeout/AbortController to dispose's drain step.",
    "[PAR-156] PAR-157 / CC-005 (blocked by PAR-156) must build the conductor's automated mirror writes against this spec: flip an explicit *.workflow.md write gate (dispatchEnabled/trackerWritePolicy/linearMutationAllowed) naming the same operation/scope/destination/receipt contract, then wire the B.5 executor outcome events to linear_import.py / single-issue updates.",
    "[PAR-156] If desired, run a live openclaw-linear-intake dry-run against the Command Central project and commit the resulting secrets-free dry-run receipt into research/ to make the dry-run gate artifact explicit (currently evidenced indirectly via plan hash + apply read-back).",
    "[PAR-156] Flip WORK-LEDGER.md PAR-156 entry from '[ ]' to done and update cc-work-ledger.json verdict (currently 'partial') once this design lands \u2014 both are outside the research/ edit scope granted here.",
    "[PAR-65] Update the stale comment at src/git-sort/deleted-file-tracker.ts:158-159 ('storage handles INSERT OR IGNORE for duplicates') to reflect full-state-sync semantics (separate ticket / out of this file-set).",
    "[PAR-65] Consider extracting a shared parameterized StorageAdapter contract test that runs against every implementation (mock + workspaceState + any future SQLite/IndexedDB) so the two suites cannot drift again.",
    "[PAR-65] PAR-65 depends_on_active PAR-64 per the ledger; confirm PAR-64 outcome does not re-touch save() semantics before closeout.",
    "[PAR-53] PAR-53 depends on PAR-52 (active) and blocks PAR-54 per the ledger \u2014 confirm ordering when the batch lands.",
    "[PAR-53] Consider whether fs.watch 'rename' events for directory recreation should trigger a full rescan; current fix handles per-file deletion eviction only.",
    "[PAR-53] Optional: have processFile clear the filenamePids entry on isValidSession()===false to avoid a tiny stale-index window if a file is rewritten to an invalid shape then deleted (currently low-risk; eviction still works because no agent is cached).",
    "[PAR-51] If a future ticket wants true Windows case-insensitivity (NTFS is case-insensitive), isPathInside could be extended to compare case-insensitively on win32 \u2014 out of scope here since the bug was purely separator handling.",
    "[PAR-51] Consider exporting isPathInside for unit-level testing if other utils need cross-platform containment, but current coverage via findRepositoryForFile is sufficient for this AC.",
    "[PAR-76] Ledger 'Consider' item (not in AC): track the setTimeout handles and clearTimeout them in dispose() at agent-decoration-provider.ts to avoid lingering timers after disposal. Left out to keep the change minimal/single-responsibility.",
    "[PAR-76] Ledger notes PAR-76 depends_on_active PAR-75 and blocks PAR-77 \u2014 coordinate merge ordering accordingly.",
    "[PAR-80] Wire `just preserve-audit` into the destructive/release recipes (cut-preview preflight, ci, dist) and any release/tag/cleanup docs so the audit is invoked as a mandatory preserve step before any destructive action \u2014 acceptance bar (re-runnable + receipt + recipe + doc) is met without it, so this was left as a follow-up to avoid touching release-recipe behavior under concurrent sibling edits.",
    "[PAR-80] Consider gitignoring research/preserve-baseline/latest.json (and dated receipts) the way .preview-status is gitignored, if receipts should not be committed.",
    "[PAR-80] PAR-81 and PAR-84 (downstream of PAR-80 in the CCSTD destructive-cleanup lane) can now reference this audit; they were not started here.",
    "[PAR-80] Optionally extend the audit to flag detached-HEAD state explicitly in the summary (currently surfaced via branch='HEAD' and upstream=null).",
    "[PAR-78] Consider making `build` always produce a fresh production VSIX regardless of whether the version already exists (decouple build-only from the versionExists gate), so a developer can rebuild a released version without bumping.",
    "[PAR-78] Record the CP-42/PAR-78 closeout in research/cc-work-ledger.json (entry still has verdict:null / empty implementation_pointers per the evidence) \u2014 outside this task's allowed file-set.",
    "[PAR-78] If WORKFLOW/CLAUDE docs reference `bun run build` semantics, note that it is now build-only (no install/release).",
    "[PAR-66] Optionally add provider-level coverage for the stopped/killed terminal transitions (L3661/3673) \u2014 these fire toasts/dock attention but intentionally do NOT emit onAgentEvent; a test documenting that 'no lifecycle event for stopped/killed' would lock in the contract.",
    "[PAR-66] Consider an explicit assertion that agent-started fires for a brand-new task that appears directly as running with a prior non-running snapshot of a DIFFERENT task, to guard the per-task previousStatuses keying.",
    "[PAR-62] PAR-68 [CP-29]: apply the same async/coalescing reload conversion to acp-session-service.ts, cron-service.ts, and taskflow-service.ts (same blocking execFileSync pattern).",
    "[PAR-62] PAR-61 [CP-21] (dependency of this ticket): openclaw-config-service.ts still wipes the agents map at the top of reload() before parsing; preserve last-known-good on parse failure.",
    "[PAR-48] Coordinate with PAR-51 [CP-09] (Windows repository matching uses POSIX separators) to extract a single shared path-normalization/containment helper instead of duplicating the .replace(/\\\\/g,'/') + separator-boundary logic across provider-factory.ts and the git-repo matching path.",
    "[PAR-48] Consider adding an explicit exact-root match test (file URI equal to the workspace root) to lock the filePath === normalizedWorkspace branch, and a mixed-separator case for completeness.",
    "[PAR-48] Update WORK-LEDGER.md / research closeout to record PAR-48 [CP-05] as done (still listed unchecked at WORK-LEDGER.md:133) once the batch gate passes.",
    "[PAR-69] PAR-68 is listed as depends_on_active for PAR-69; PAR-67 and PAR-70 are blocked by it \u2014 confirm the escape-hatch contract here aligns with those tickets' expectations before they land.",
    "[PAR-69] Consider extracting these snapshot-projection regressions into a dedicated bun:test unit suite (parallel to lane-registry-projection.test.ts) so getTreeSnapshotForProvider cap/escape-hatch behavior is covered without the live VS Code host overhead.",
    "[PAR-69] The installed-VSIX proof harness (test/integration/installed-vsix-proof-harness.test.ts) asserts the integration-test-api contract; verify the new requiredLabels-beyond-cap behavior is reflected there if that proof exercises capped trees.",
    "[PAR-77] If getCurrentChanges and getCurrentChangesUnfiltered are meant to stay permanently identical, consider collapsing them or having getCurrentChanges delegate to a single unfiltered core to remove the duplication and the latent-divergence risk entirely.",
    "[PAR-77] WORK-LEDGER.md still lists PAR-77 unchecked (per ledger evidence); update the checkbox after gates pass (outside this task's allowed file-set).",
    "[PAR-77] Broader coverage: add a sibling test for discoverExtensions() (initial populate path) to lock in that both discovery paths use the unfiltered method, preventing future regressions in either direction.",
    "[PAR-73] Consider skipping creation of the StatusBarItem entirely when showInStatusBar is false (current fix hides; the item is created lazily only when shown, so no leak, but a tiny optimization is possible).",
    "[PAR-73] There is no test asserting a false->true re-enable path restores the icon; could add for completeness, though refresh()'s else-if branch already covers it.",
    "[PAR-55] AgentOutputChannels uses execFileSync (synchronous, blocking) inside the interval \u2014 same blocking-CLI class of issue as PAR-68/CP-29 for the other services; could be migrated to async execFile with a timeout/AbortController in a follow-up.",
    "[PAR-55] Consider injecting setIntervalImpl/execFileSyncImpl (DI) like infrastructure-health-status-bar does, so streaming-cadence tests don't need to monkey-patch globalThis.setInterval.",
    "[PAR-55] No type export of the streaming state is surfaced; a getActiveSessionId(taskId) accessor could make session-swap behavior observable to callers and easier to test without internals access.",
    "[PAR-81] Consider whether the gate should also forbid out/ and prompts/ (gitignored generated/dev categories not currently in .vscodeignore) \u2014 would require first adding them to .vscodeignore to keep the gate's mirror contract consistent.",
    "[PAR-81] FORBIDDEN_SUFFIXES covers .map/.log/.vsix/.tsbuildinfo but not other gitignored artifact suffixes (*.tgz, *.lcov, *.bak, *.backup, *.old, *.orig); low risk but could be aligned if a leak ever appears.",
    "[PAR-81] Optional: add an automated cross-check test that diffs .vscodeignore directory entries against FORBIDDEN_DIR_PREFIXES so future drift (a new .vscodeignore dir not mirrored in the gate) fails CI instead of relying on manual audit.",
    "[PAR-84] Wire --require-push-target into a justfile recipe (e.g. prerelease-gate-integrated and/or cut-preview) so the guardrail runs automatically on the release path (justfile was outside this ticket's allowed file-set).",
    "[PAR-84] Run the live verification: a real scripts/release.sh invocation against a deliberately-misconfigured origin to confirm the exit 1, and a live `just prerelease-gate --require-push-target` against the real repo (expected pass since origin matches package.json).",
    "[PAR-84] Consider documenting the split-identity Tier-2 / push-target assertion in WORKFLOW.md and STANDARDS.md (both outside this ticket's allowed file-set) per the implementation_pointers, separating local build/install from public publish/tag actions.",
    "[PAR-84] Consider factoring the normalize-github-remote logic into a single shared source consumed by both the TS gate and bash release script to avoid drift.",
    "[PAR-67] Extract a single shared watch-with-retry utility (e.g. under src/utils/) and have all three services consume it, removing the triplicated logic introduced here.",
    "[PAR-67] Add dedicated watcher-retry regression tests for AcpSessionService and TaskFlowService mirroring the cron-service test, since they share the same fix but currently have no watch-retry coverage.",
    "[PAR-67] Consider bounded/backoff retry instead of a fixed 5s interval, and a way to surface persistent watch failure to the user rather than polling indefinitely.",
    "[PAR-44] PAR-45 is blocked by PAR-44 (blocks_active) \u2014 re-verify it once this lands.",
    "[PAR-44] Consider auditing other process-exec or path-type contributed settings for trust-gating coverage as a follow-up hardening pass, now that the manifest-contract test enforces restricted keys must exist.",
    "[PAR-228] PAR-231 (blocked by this) can consume PaneAttentionState to render a per-row attention-reason badge + pane/session evidence in the tooltip (the ledger's createTaskItem/detail-builder pointer) beyond the current benign 'live shell' badge.",
    "[PAR-228] Consider surfacing awaiting-user-input as its own first-class reason/icon distinct from the generic lifecycle-conflict badge.",
    "[PAR-228] Expand COMPLETION_SUMMARY/AWAITING_INPUT patterns from real captured fixtures (pytest/cargo/go test/gum/fzf selectors) and consider a fixture corpus test.",
    "[PAR-228] If capture cost on large lane sets becomes a concern, gate capturePaneSnippet behind a config toggle or widen the cache TTL.",
    "[PAR-70] Consider awaiting flushPersistence() in the extension deactivate path so pending filter writes are durably flushed on shutdown (requires editing extension.ts / the owning provider, outside this ticket's file-set).",
    "[PAR-70] Optionally apply the same serialized-write-chain pattern to other fire-and-forget workspaceState/globalState persisters in the codebase if they exhibit the same out-of-order risk (sibling concurrency tickets PAR-67/68/69).",
    "[PAR-70] If knip flags flushPersistence as unused, either add a knip ignore annotation or wire it into a real caller (deactivate flush) to justify the export.",
    "[PAR-227] Wire GC receipt emission into ghostty-launcher scripts/lib/work-system-bridge.sh + reaper.sh (the authoritative lanes.json producer) so the whole-read-model rebuild/GC runs on the live path, then flip Command Central from opt-in env read to a default read of the launcher's real receipt path.",
    "[PAR-227] Add a Command Central config setting (commandCentral.laneRegistry.gcReceiptPath) and/or a command to surface the GC receipt for audit (rows kept/downgraded/archived/removed + reasons), since gc_reconcile_reason is captured but not yet shown in the tree item description.",
    "[PAR-227] Add an integration test that drives the full loop: oste-lanes-gc.sh emits a receipt, Command Central ingests lanes.json + receipt via CC_LANE_GC_RECEIPT, and a downgraded row renders in Needs Review.",
    "[PAR-227] Consider de-duplicating the inline provider reconcile against the exported applyGcReceiptReconciliation once the provider/normalize module boundary is revisited.",
    "[PAR-227] Extend oste-archive.sh / reaper.sh (in ghostty-launcher) to consult or co-emit the GC receipt so archive/settle paths and the GC pass share one row-evaluation source.",
    "[PAR-45] If autoDismiss behavior is genuinely wanted, reintroduce the setting and implement it via a withProgress-based notification (which the extension can resolve after N seconds) or a setStatusBarMessage(text, autoDismissSeconds*1000) echo on the completion path in agent-status-tree-provider.ts \u2014 not via showInformationMessage.",
    "[PAR-45] Add full enable<->disable lifecycle support for the cron feature (dispose service/view/commands on disable, re-activate on enable) so toggling cron.enabled at runtime fully releases the openclaw cron watcher without a reload, matching a true on/off contract.",
    "[PAR-45] Consider a package-json contribution test that asserts every contributed commandCentral.* setting has at least one runtime consumer (would have caught both CP-02 orphans automatically and guards against regressions).",
    "[PAR-45] PAR-45 depends_on_active PAR-44 in the ledger; confirm PAR-44 is resolved before closing PAR-45 in Linear.",
    "[PAR-58] Commit the working-tree change for PAR-58 (package.json + manifest-contract.test.ts) once the orchestrator gate (just check / bun test) passes, so HEAD no longer ships the stale untrusted-workspace restrictions.",
    "[PAR-58] Consider a broader manifest-consistency assertion that flags ANY machine/process-exec-scoped setting (e.g. scope: machine paths) that is NOT in restrictedConfigurations, to catch future launcher/exec settings that should be trust-gated \u2014 currently only the inverse (stale -> not-contributed) is checked.",
    "[PAR-58] Confirm with PAR-57/PAR-59 owners that commandCentral.ghostty.launcherPath is the only external-launcher-affecting setting needing restriction; if future tickets add more exec-affecting settings they must be added to restrictedConfigurations too.",
    "[PAR-157] PAR-158 / CC-006 (which CC-005 blocks): implement the conductor daemon process, conductor.config.jsonc, conductor/prompts/cc-lane.md, and the WORKFLOW.md Conductor section against this contract.",
    "[PAR-157] Flip the OpenClaw *.workflow.md write-gate (observe -> dispatch) as a separate operator-approved change before the conductor may launch or mirror unattended.",
    "[PAR-157] Optional small extension follow-on (own ticket): add a conductor-health dimension (mode + last-poll age) as a tooltip line on the CC-002 six-state infrastructure-health-status-bar, plus a read-only receipt-freshness evidence row in the Symphony surface.",
    "[PAR-157] Land the launcher -> OpenClaw ledger write path (CC-OPENCLAW-NATIVE-LOOP target: launcher spawn calls 'openclaw tasks register') so LAUNCH-step run attempts appear as first-class CodexRunView rows rather than inferred launcher entries.",
    "[PAR-82] Parse extension/package.json from the built VSIX zip in vsix-content-gate so the Workspace Trust gate is fully hermetic against the shipped artifact.",
    "[PAR-82] Add a caller-side friendly notification in extension.ts when WorkspaceTrustRequiredError is thrown (e.g. an actionable 'Trust workspace' toast), once that file is in scope.",
    "[PAR-82] Consider extending SUBPROCESS_SPAWNING_SETTINGS / restrictedConfigurations if future settings drive subprocess execution (currently only commandCentral.ghostty.launcherPath does).",
    "[PAR-82] Map an extension-host startup-smoke assertion into prerelease-gate.ts (the ledger mentions tasks-json-startup-smoke harnesses) so runtime activation is gated alongside the manifest posture.",
    "[PAR-59] Consider a follow-up (needs src/discovery/types.ts + tree provider, outside this file-set) to add an explicit 'stuck'/'stale' status field on DiscoveredAgent so README's 'stuck agents at a glance' wording is honored with distinct UI surfacing, rather than retaining aged live agents as ordinary running agents.",
    "[PAR-59] DEFAULT_STALE_PROCESS_AGE_MS (4h) is now only a pruning trigger for DEAD aged PIDs; if product wants a true 'has been running too long' soft warning for live long-running externals, that would be a separate signal.",
    "[PAR-59] PAR-59 depends_on_active lists PAR-57/PAR-58; re-verify no interaction once those land (they were not in scope here).",
    "[PAR-158] Write the CC-006 closeout report under research/ consolidating the existing PAR-189/191/193/195 dogfood markers against the four acceptance criteria, and either close PAR-158 or formally mark it superseded by those runs (outside this file-set).",
    "[PAR-158] Run one real ready-for-agent Linear issue through the full loop and visually confirm the new Tracked issue / Execution node rows populate from live OpenClaw projection data (the live-issue portion this ticket is marked partial for).",
    "[PAR-158] Verify the upstream OpenClaw task projection (OpenClawTaskService / Work System feed) actually populates trackerKind, issueUrl, workspacePath, execNodeName, nodeConnected for cross-project lanes; if not, that population is the remaining cross-repo work.",
    "[PAR-158] Consider also surfacing the tracked-issue identity in the Codex Run Attempts projection (CodexRunView) so cross-project issues are visible in the Symphony Run Attempts container, not only under the OpenClaw task detail rows.",
    "[PAR-231] Consider surfacing the captured pane snippet + why-benign evidence in the confirmation modal (or a propose/preview panel) so the operator sees the exact pane state before approving, matching the ledger's richer propose/approve/execute vision.",
    "[PAR-231] Consider a batch closeout (toolbar action) that proposes all benign live panes at once with a per-pane evidence list, paralleling reapStaleAgents.",
    "[PAR-231] If a dedicated 'closed_out' lifecycle status is desired (vs reusing 'failed'), add a markTaskClosedOutInRegistryMap helper in agent-task-registry.ts and a matching status badge in the tree provider.",
    "[PAR-54] Consider a shared helper so AgentRegistry.readLauncherTasks and the tree provider's getConfiguredSources/readRegistry derive their resolved sources from a single code path, eliminating the two parallel agentTasksFiles/laneRegistry config-reading implementations.",
    "[PAR-54] AgentRegistry.readLauncherTasks does not consult laneRegistry.files (lane-records-only ingest) the way the display path does; if discovery suppression should also account for registry-backed lane records, that is a separate, larger contract change beyond CP-12's additional-registry scope."
  ]
}