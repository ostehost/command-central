# RESULT — cc-rc53-preview-cut-fable-20260610

Cut the 0.6.0-rc.53 preview locally via the standard `just cut-preview` path
to exercise the reviewed release-digest base hardening (`0f131f3d` +
`3487acaa`) in the real prerelease flow — specifically the deterministic
git-derived "Since previous prerelease cut" digest section with the hardened
`--fixed-strings` + `isCutCommit(subject)` base resolution. Local-only: no
push, no tags, no Marketplace publish, no external comms.

## Preconditions verified

- command-central tree clean at start; HEAD `3487acaa`
  (`docs(research): add release-digest base hardening receipt`), with the
  hardening fix `0f131f3d` as ancestor. Approved clean by
  `review-cc-release-digest-base-hardening-fable-20260610` per task brief;
  the committed hardening receipt
  `research/RESULT-cc-release-digest-base-hardening-fable-20260610.md`
  documents the fixes.
- ghostty-launcher tree clean on committed HEAD `3cab35c4`.
- Hub machine (`ostehost@MacBookPro`), Bun 1.3.13.
- Starting version `0.6.0-rc.52` → expected cut rc.53. ✅

## Commands run

```bash
just cut-preview                 # preflight → sync-launcher → just ci rehearsal
                                 # → prerelease-gate → dist --prerelease (56.4s)
just vsix-gate                   # explicit re-run of the content gate
just verify-vscode-consumption --vsix releases/command-central-0.6.0-rc.53.vsix \
  --expected-version 0.6.0-rc.53 \
  --manifest-out research/vscode-consumption-rc53-20260610.json
just preview-status              # exit code 0, SUCCEEDED
```

## Gates

| Gate | Result |
| --- | --- |
| Preflight (both repos clean, hub host) | ✅ |
| `sync-launcher` (binary 1.2.8 in sync; 36 helper files refreshed, byte-identical) | ✅ |
| `just ci` rehearsal — Biome 250 files, tsc, knip, quality checks | ✅ |
| Test suite | ✅ 1767 pass / 1 skip / 0 fail (125 files, 12.16s) |
| `prerelease-gate` (CC `just ci` + launcher `just check` + contract checks) | ✅ `research/prerelease-gate/prerelease-gate-2026-06-10T16-09-34.991Z.json` |
| VSIX content gate (in-path during dist) | ✅ budgets respected, no forbidden artifacts |
| `just vsix-gate` (explicit re-run) | ✅ identical numbers |
| Independent forbidden-pattern scan (`unzip -l` grep for `.map`, tests, coverage, `node_modules`, `.ts`, `src/`, `scripts-v2`, tsconfig) | ✅ 0 hits |
| `window-probe.applescript` present in package | ✅ `extension/resources/bin/scripts/lib/window-probe.applescript` (2,058 bytes) |

## Artifact identity

- **VSIX:** `releases/command-central-0.6.0-rc.53.vsix`
- **Version:** `0.6.0-rc.53` (publisher `oste`, name `command-central`)
- **Compressed:** 265,983 bytes (259.7 KB) — budget 600,000
- **Uncompressed:** 884,312 bytes — budget 2,000,000
- **Files:** 52 — budget 120 (unchanged from rc52)
- **sha256:** `e93aca82e72cb70d6aa7c01525cc70270104b733ef9794e299543e75080fa2d8`
- **Digest:** `releases/digest-v0.6.0-rc.53.md` (tracked record; VSIX itself is gitignored)
- `preview-status`: SUCCEEDED, pid 3359, 2026-06-10T16:08:43Z → 16:09:40Z
  (56.4s), artifact sha matches the independently computed sha256 above.

## Digest freshness proof (objective of this cut)

`releases/digest-v0.6.0-rc.53.md` ends with a fresh, deterministic section
generated by the hardened base resolution — not stale static-only text:

```
📦 **Since previous prerelease cut (rc52)**
  • `0f131f3d` fix(release-digest): harden previous-cut base resolution
  • `6a2e57a5` fix(release-digest): append git-derived since-previous-cut section
```

- The base resolved correctly to the rc52 cut commit `6d052d21`
  (`chore(release): cut rc52 preview`) via the hardened
  `--fixed-strings` grep + authoritative `isCutCommit(subject)` check.
- The two commits listed are exactly the user-visible (`fix:`) commits in
  `6d052d21..HEAD`. The other two commits in that range — `edf03cd3` and
  `3487acaa` — are `docs(research):` receipts, excluded by the generator's
  documented subject filter (`scripts-v2/release-digest.ts:42`). This is the
  intended filter behavior, not staleness.
- Net effect: the hardening commits themselves are the payload of the section
  they harden — the feature proved itself on its own commits.

## Install status

`code --install-extension` ran inside dist against VS Code 1.122.1 (arm64);
`verify-vscode-consumption` confirms `oste.command-central@0.6.0-rc.53`
installed with package sha256 matching the release artifact
(`research/vscode-consumption-rc53-20260610.json`, `success: true`).
Interactive smoke test (Reload Window, Agent Status tree, Focus Terminal) is
left to the user per the cut-preview handoff checklist.

## Sync churn provenance

`sync-launcher` refreshed 36 helper files in `resources/bin/scripts/`; all
were byte-identical to launcher HEAD `3cab35c4` (no working-tree diff), so
the cut carries no launcher changes. Release archive rotation removed the
gitignored `command-central-0.6.0-rc.50.vsix` (keep-last-3 policy).

## Commits

- `chore(release): cut rc53 preview` — version bump, digest, gate artifacts,
  consumption manifest. Hooks passed; no `--no-verify`.
- `docs(research): add rc53 preview cut receipt` — this file.

## Hard-stop compliance

No push, no tags, no Marketplace publish, no external GitHub release changes.
The full flow is local; dist's only outward-looking step is the local
`code --install-extension`.
